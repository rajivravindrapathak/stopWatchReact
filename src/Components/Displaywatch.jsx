import React from 'react'

const Displaywatch = (props) => {
    return (
        <div>
            <span>{props.time.h}</span>
            <span>{props.time.h}</span>
            <span>{props.time.h}</span>
            <span>{props.time.h}</span>
            <span>{props.time.h}</span>
        </div>
    )
}

export default Displaywatch
